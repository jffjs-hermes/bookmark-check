# A healthy document: only code blocks, relative refs and out-of-scope schemes.

```text
This whole line is code, so https://ignored.example/inside is not extracted.
```

A [relative link](docs/guide) and a mailto:[contact](mailto:a@b.example) are out of scope.